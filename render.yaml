services:
  - type: web
    name: language-learner-pro
    env: python
    rootDir: LanguageLearner
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: PORT
        value: 10000
